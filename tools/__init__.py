"""Exploration tools for the photo recovery pipeline.

Tools are created as needed during rule development:

- Threshold tuner: Side-by-side pairs at each hamming distance level
- Directory structure explorer: Pruned tree view with file counts
- Group browser: View photos grouped by perceptual similarity
- Directory browser: View photos grouped by parent directory
- Stats dashboard: Counts by stage, by rule, by decision type
"""
